# Presentations

Marp slide decks (`.marp` / `.md` with marp frontmatter) live here. One file per deck. Render with the [Marp CLI](https://github.com/marp-team/marp-cli) or the VS Code Marp extension.
