*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.kotlin
.cxx
local.properties
/app/build
/app/release
*.apk
*.aab
*.jks
*.keystore

# Code intelligence index (generated by codanna MCP server)
.codanna/

# ML model embedding cache (generated by codanna MCP server)
.fastembed_cache/

# Local environment config (not committed — copy and adapt per machine)
.envrc

# Claude Code local artifacts
.claude/settings.local.json
.claude/reviews/
.claude/plans/
.claude/bug-reports/

# Claude sandbox mounts (read-only stubs surfaced by the sandbox runtime)
.claude/agents
.claude/commands
.claude/settings.json
.claude/skills

# Codanna local config
.codannaignore

# Temporary debugging artifacts (screenshots, bug reports)
/tmp/

# Home dotfiles surfaced into the working tree by the Claude sandbox
# (read-only mounts of $HOME config — never project files)
/.bash_profile
/.bashrc
/.gitconfig
/.profile
/.ripgreprc
/.vscode
/.zprofile
/.zshrc
